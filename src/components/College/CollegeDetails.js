import React from 'react'

function CollegeDetails(match ) {
    return (
        <div style={{color:"white"}}>
            Hello there. You must be {match.id}
        </div>
    )
}

export default CollegeDetails
